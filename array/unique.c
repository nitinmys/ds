#include <stdio.h>

find_unique (int arr[], startindex, endindex)
{
    if (endindex - startindex == 1)
    {
        if (arr[startindex] != arr[endindex])
        {
            printf ("
int main ()
{
    int arr[] = {1, 1, 2, 2, 3, 3, 4, 50, 50, 65, 65};
    int n = sizeof(arr)/sizeof(int);
    int i;
    find_unique (arr[], 0, n-1)
; .
